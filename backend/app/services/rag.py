def index_client_notes(client_id: str, notes: str):
    """Stub — RAG indexing disabled for lightweight deployment."""
    pass


def retrieve_context(agent_id: str, query: str, top_k: int = 5) -> str:
    """Stub — returns empty context; RAG can be re-enabled later."""
    return ""
