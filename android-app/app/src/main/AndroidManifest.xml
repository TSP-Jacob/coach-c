<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Needed for the in-browser voice features (lib/voiceClient.ts's
         getUserMedia call, notes dictation) — the WebView forwards its own
         permission prompt to this app's runtime permission. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Chromium's audio engine requires this (alongside RECORD_AUDIO) to
         select an input device at all — without it, getUserMedia() fails with
         NotReadableError even though the mic permission itself is granted.
         Confirmed via logcat: "media/audio/android/audio_manager_android.cc:883
         Unable to select communication device!" / "Requires
         MODIFY_AUDIO_SETTINGS and RECORD_AUDIO." This is a normal (non-
         dangerous) permission — granted automatically at install, no runtime
         prompt needed. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.CoachC.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
